/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
/.claude

# Локальная SQLite-БД (используется только для разработки без Docker)
/database/*.sqlite
/database/*.sqlite-journal
# Если при переключении на SQLite забыть поправить DB_DATABASE, файл БД
# создастся в корне с именем из .env — не тащим его в репозиторий.
/deposit_desk
